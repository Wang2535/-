# B组 第四轮操作，核心主题：地图数据最终验证与质量报告

## 📊 当前开发状态

**完成度：95% ✅ 9层数据完整，验证脚本已创建**

### 第三轮已完成
- ✅ 9层地图数据全部创建
- ✅ `runFullValidation.ts` 验证脚本已创建
- ✅ 导入路径已统一为 `../../types`
- ✅ 格子ID使用 `R{row}C{col}` 统一格式

### 🔴 仍存在的问题

#### 问题1：`runFullValidation.ts` 需要实际运行并输出报告
脚本已创建，但需要：
- 确保能正确导入所有9层数据
- 运行并生成完整的验证报告
- 确认layer6~layer9的数据质量

#### 问题2：9层数据质量报告未生成
需要一份综合报告，包含：
- 每层的格子数量统计
- 连通性验证结果
- 区域分布统计
- 功能格数量核对

---

## 🎯 本轮任务

### 任务1：运行 `runFullValidation.ts` 并修复任何问题

```bash
cd game-temp && npx ts-node src/tower-mode/data/runFullValidation.ts
```

如果运行失败，修复以下可能的问题：
1. 导入路径是否正确？
2. 是否正确导入了全部9层数据？
3. BFS连通性检查逻辑是否完整？

### 任务2：生成9层数据质量报告

运行验证脚本后，输出格式：

```
=== 爬塔模式 9层地图数据质量报告 ===
生成时间: 2026-XX-XX XX:XX:XX

┌─────────┬─────────────────────┬──────┬──────────┬───────────┬──────────┬────────┐
│ 层级    │ 主题                │ 格子 │ 战斗格   │ 功能格    │ BOSS格   │ 连通性 │
├─────────┼─────────────────────┼──────┼──────────┼───────────┼──────────┼────────┤
│ Layer 1 │ virus-lab           │ 13   │ 8        │ 4         │ 1        │ ✅     │
│ Layer 2 │ cyberspace          │ 17   │ 11       │ 5         │ 1        │ ✅     │
│ Layer 3 │ data-vault          │ 13   │ 7        │ 5         │ 1        │ ✅     │
│ Layer 4 │ grid-city           │ 11   │ 6        │ 4         │ 1        │ ✅     │
│ Layer 5 │ smart-factory       │ 16   │ 10       │ 5         │ 1        │ ✅     │
│ Layer 6 │ mobile-terminal     │ ??   │ ??       │ ??        │ 1        │ ?      │
│ Layer 7 │ cloud-platform      │ ??   │ ??       │ ??        │ 1        │ ?      │
│ Layer 8 │ future-lab          │ ??   │ ??       │ ??        │ 1        │ ?      │
│ Layer 9 │ command-center      │ ??   │ ??       │ ??        │ 1        │ ?      │
└─────────┴─────────────────────┴──────┴──────────┴───────────┴──────────┴────────┘

总计:
  总格子: XXX (目标 ~126)
  总战斗格: XX (目标 81)
  总功能格: XX
  总BOSS格: 9
  连通性: 9/9 ✅

区域分布:
  W区(虚弱): XX格
  N区(知识): XX格
  I区(反转): XX格
  P区(休整): XX格
  S区(加速): XX格
  D区(危险): XX格
```

### 任务3：核对与C组的对接数据

确认每层的战斗格数量，供C组关卡分配使用：

```typescript
// 输出格式
const layerBattleCellCounts = {
  1: 8,   // Layer 1 有8个战斗格
  2: 11,  // Layer 2 有11个战斗格
  3: 7,   // ...
  4: 6,
  5: 10,
  6: ?,   // 待确认
  7: ?,
  8: ?,
  9: ?,
};
```

### 任务4：修复发现的任何问题

如果在验证中发现：
- 某层存在不可达格子 → 修复 `adjacencyList` 或 `paths`
- 某区域 `cellIds` 指向不存在的格子 → 修正区域定义
- 功能格数量不符合预期 → 调整格子类型

---

## ✅ 完成标准

- [ ] `runFullValidation.ts` 成功运行
- [ ] 9层地图连通性100%通过
- [ ] 输出完整的数据质量报告
- [ ] 提供每层的战斗格数量供C组使用
- [ ] 修复所有发现的数据问题

---

> **文档版本**: v4.0 (第四轮)
> **本轮目标**: 地图数据最终验证与质量确认
